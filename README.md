# React native + Expo + Expo Sqlite + NativeWind

This app is basic app for get start with React native + Expo + Expo Sqlite + NativeWind technologies. Its easy to get started to react native developments
